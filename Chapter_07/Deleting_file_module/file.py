# Deleting a File
"""
using the os module
Module (like a code library) is a file by another programmer that generally has a functions we can use.
Some modules are preInstalled by python. But somes are not installed in order to install them we use pip (package installer for python). pip install Module_name or pip3 install _module_name

Syntax:
import os
os.remove(filename)
"""

import os

# Deleting a file
# os.remove("demo.txt")
